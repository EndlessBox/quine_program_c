#include <stdio.h>
#define decode(t, n, a, k, e, t1) a##e##k##n
#define liJaBismillah decode(z, n, m, i, a, t)
#define CLONE(x) int liJaBismillah() { void* ptr = fopen("second_kid.c", "w"); fputs(x, ptr); fputs("\n\n/*\n     El Caid Motorhead \\,,/(>.<)\\,,/\n*/\n\nCLONE("#x");", ptr);}

/*
     El Caid Motorhead \,,/(>.<)\,,/
*/

CLONE("#include <stdio.h>\n#define decode(t, n, a, k, e, t1) a##e##k##n\n#define liJaBismillah decode(z, n, m, i, a, t)\n#define CLONE(x) int liJaBismillah() { void* ptr = fopen(\"second_kid.c\", \"w\"); fputs(x, ptr); fputs(\"\\n\\n/*\\n     El Caid Motorhead \\\\,,/(>.<)\\\\,,/\\n*/\\n\\nCLONE(\"#x\");\", ptr);}");